[workspace]

[package]
name = "rust_keyword_lsp_server"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "*", features = ["full"] }
tower-lsp = "*"

# pest
pest = "*"

# asm lib
riscv_asm_lib = { path = "../../riscv_asm_lib" }